# Unity 逆向课程 - 第四课 配套资源 / 授权靶场
#
# GitHub 普通 Git 推送限制：单文件超 50MB 告警，超 100MB 直接拒绝推送。
# Assets/Mesh 下的资源使用双向控制字符（U+206A/U+202B/U+200F/U+206E 等）混淆命名，
# 文件名不可读、也无法逐条书写精确规则，因此该目录下的 *.asset 整体交给 Git LFS。
# 其中体积最大的一个文件为 145.9MB，已超过 100MB 硬限，必须走 LFS 才能推送。
#
# 规则必须写仓库内相对路径；绝对路径（如 F:/...）永远不命中任何文件，LFS 会静默失效。
# 注意：gitattributes 的 pattern 不支持空格转义，含空格的路径无法用规则匹配。
授权靶场/Assets/Mesh/*.asset filter=lfs diff=lfs merge=lfs -text
